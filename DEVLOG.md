# 开发日志：连线拖拽创建节点功能

## 功能目标
实现从节点连接点拖出连线到空白处时，弹出"创建节点"菜单，用户选择后自动创建节点并建立连接。

## 开发过程

### 1. 基础实现
- 添加 `ConnectEndMenuState` 状态管理菜单显示
- 实现 `onConnectEnd` 回调检测连接结束事件
- 创建菜单UI提供"对话"、"笔记"、"取消"三个选项

### 2. 问题排查与修复

**问题一：onConnectEnd 未触发**
- 原因：React Flow 的 `connectionState` 使用 `fromNode`/`fromHandle`，而非 `source`/`sourceHandle`
- 解决：修正参数结构，正确读取连接状态

**问题二：连接点语义错误**
- 原因：未区分左右连接点（引用关联）和上下连接点（层级传递）
- 解决：根据 `sourceHandle` 类型智能选择新节点的连接点方向

**问题三：连接类型显示错误**
- 原因：`getConnectionType(sourceHandle, null)` 传入 null 导致判断失败
- 解决：直接根据连接点类型判断 `relationType`

## 最终效果
- 从左右连接点拖出 → 创建节点并建立"引用关联"连线
- 从上下连接点拖出 → 创建节点并建立"层级传递"连线
- 保持连接点语义一致性

## 技术要点
- React Flow `onConnectEnd` 回调的 `FinalConnectionState` 类型结构
- 连接点命名规范：`{direction}-{type}`（如 `left-source`、`top-target`）
- 连接类型判断逻辑的边界处理


---

## 2026年3月8日 · 学习计划识别优化

今天的工作从一条用户反馈开始——学习计划识别质量不够理想，提取的信息过于简陋。

### 问题诊断

打开 `/api/plan/extract` 路由，发现之前使用的是基础模型，Prompt 也比较简单，只提取标题、描述和预估时间。用户在画布中创建的节点内容单薄，缺乏指导价值。

### 模型与Prompt升级

首先将模型升级为旗舰版 `doubao-seed-2-0-pro-260215`，这是豆包系列的最强模型。然后重写 Prompt，要求提取更丰富的信息：
- 整体学习目标（overallGoal）
- 前置知识要求（prerequisites）
- 每个步骤的关键词标签（keywords）
- 每个步骤的学习目标（goals）

测试时遇到一个小插曲：最初用简单文本测试返回 `hasPlan: false`，排查后发现 API 需要传入完整的对话消息数组，而非单条输入。修正后，API 成功返回了结构化的计划数据，每个步骤都包含关键词和学习目标，信息量大大提升。

### 组件与状态同步

接下来更新 PlanDetector 组件，新增显示整体目标、前置知识、关键词标签。同时更新 `canvasStore.ts` 中的类型定义，确保前后端数据结构一致。

### 交互细节打磨

用户截图反馈：学习计划面板默认展开会遮挡对话区域，影响体验。这个建议很中肯——用户可能想继续对话，而不是立即查看计划。

将默认状态改为收起，并优化交互：
- 点击整个头部区域切换展开/收起
- 收起时显示"点击展开查看"提示
- 关闭按钮独立处理，避免事件冒泡

### 小结

这次优化体现了"渐进式披露"的设计原则：不强制用户关注计划，而是提供一个可发现、可忽略的入口。技术层面，模型升级和 Prompt 工程是核心，但最终决定体验的还是对用户行为习惯的理解。

---

