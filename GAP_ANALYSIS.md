# 画布化对话系统 - 差距分析与改进计划

## 📊 愿景 vs 现状对比

### ✅ 已完成（符合设计文档）

| 模块 | 功能 | 状态 | 备注 |
|------|------|------|------|
| 画布基础 | 无限平移缩放 | ✅ 完成 | React Flow 内置 |
| | 网格背景 | ✅ 完成 | 点状网格 |
| | 小地图导航 | ✅ 完成 | 右下角 |
| 节点管理 | 添加节点 | ✅ 完成 | 双击/工具栏 |
| | 删除节点 | ✅ 完成 | 菜单操作 |
| | 节点拖拽 | ✅ 完成 | 自由移动 |
| | 节点折叠 | ✅ 完成 | 标题栏按钮 |
| | 四向连接点 | ✅ 完成 | 上/下/左/右 |
| 聊天功能 | 流式输出 | ✅ 完成 | SSE协议 |
| | 多轮对话 | ✅ 完成 | 节点内完整上下文 |
| | Markdown渲染 | ✅ 完成 | 自定义渲染器 |
| 连线系统 | 创建连线 | ✅ 完成 | 拖拽端口 |
| | 删除连线 | ⚠️ 部分 | 需要优化 |
| | 连线样式 | ✅ 完成 | 贝塞尔曲线+动画 |
| 数据持久化 | LocalStorage | ✅ 完成 | 自动保存 |
| | 导出 | ✅ 完成 | JSON格式 |
| 扩展功能 | 大纲视图 | ✅ 完成 | 树形列表 |
| | AI建议 | ✅ 完成 | 基于对话生成 |
| | 节点分组 | ✅ 完成 | GroupNode |
| | 标签管理 | ✅ 完成 | 添加/删除 |
| | 系统提示词 | ✅ 完成 | 每节点独立设置 |
| | 学习状态 | ✅ 完成 | 3种状态标记 |
| | 知识摘要 | ✅ 完成 | AI生成 |
| | @引用 | ✅ 完成 | 快捷引用节点 |

### ❌ 未完成（与设计文档有差距）

| 模块 | 功能 | 优先级 | 说明 |
|------|------|--------|------|
| **树状布局** | 自动布局 | 🔴 高 | 用户期望的"向下生长"树形布局 |
| | 布局切换 | 🟡 中 | 树形/自由布局切换 |
| **连线系统** | 连线类型区分 | 🔴 高 | 父子连线 vs 兄弟连线视觉区分 |
| | 连线语义 | 🔴 高 | 上下连接=层级，左右连接=引用 |
| | 连线管理面板 | 🟡 中 | 查看/管理所有连线 |
| **交互增强** | 框选多选 | 🟡 中 | 设计文档P1功能 |
| | 键盘快捷键 | 🟡 中 | Delete删除、Ctrl+C/V复制 |
| | 撤销/重做 | 🟢 低 | 操作历史管理 |
| **节点增强** | 节点搜索 | 🟡 中 | 大量节点时快速定位 |
| | 节点复制 | 🟡 中 | Ctrl+C/V复制节点 |
| **渲染优化** | 代码高亮 | 🟡 中 | 代码块语法高亮 |
| | 流式光标 | 🟢 低 | 打字机效果优化 |
| **导入功能** | JSON导入 | 🟡 中 | 导入JSON恢复画布 |
| **初始体验** | 空画布引导 | 🟡 中 | 首次使用的引导 |

---

## 🎯 核心痛点分析

### 1. 树状布局缺失（最重要）

**用户期望**：
> "预想中的排布更像是树状的（有点像向下生长的二叉树）"

**当前问题**：
- 节点位置完全由用户手动拖拽
- 没有"自动排列成树形"的功能
- 连线关系建立后，节点位置不会自动调整

**解决方案**：
1. 添加"自动布局"按钮，一键将节点排列成树形
2. 支持多种布局算法：树形（垂直/水平）、力导向、网格
3. 连线创建时可选择"自动排列子节点"

### 2. 连线语义不清晰

**用户期望**：
> "父子节点，兄弟节点之间如何通信交互...还有待开发探索"

**当前问题**：
- 四个连接点已实现，但语义不明确
- 上下连接（紫色）和左右连接（靛蓝）只是颜色区分
- 没有明确"父子关系"vs"引用关系"的概念

**解决方案**：
1. 明确定义连接语义：
   - **顶部/底部连接 = 层级关系**（父子）
   - **左侧/右侧连接 = 引用关系**（兄弟/上下文）
2. 连线样式区分：
   - 层级连线：实线 + 箭头 + "父→子"标签
   - 引用连线：虚线 + "上下文"标签
3. 节点UI显示关系：
   - 显示"父节点：XXX"
   - 显示"子节点：A, B, C"

### 3. 交互不够流畅

**当前问题**：
- 删除连线需要选中后按Delete，不够直观
- 没有框选多选功能
- 缺少常用快捷键

**解决方案**：
1. 连线hover时显示删除按钮
2. 启用框选多选（React Flow内置支持）
3. 添加快捷键：
   - `Delete` 删除选中节点/连线
   - `Ctrl+C/V` 复制/粘贴节点
   - `Ctrl+Z/Y` 撤销/重做
   - `Ctrl+F` 搜索节点

---

## 📋 改进计划

### 阶段一：核心体验优化（优先级最高）

**目标**：解决用户最关心的树状布局和连线语义问题

#### 1.1 树状布局系统
```
任务清单：
□ 实现自动布局算法（dagre.js）
□ 添加"自动布局"按钮到工具栏
□ 支持3种布局模式：垂直树、水平树、力导向
□ 新建子节点时自动排列位置
□ 布局动画过渡效果
```

#### 1.2 连线语义明确化
```
任务清单：
□ 定义两种连线类型：
  - hierarchy（层级连线）：顶部→底部
  - reference（引用连线）：左侧→右侧
□ 不同类型连线使用不同样式：
  - 层级连线：实线、粗、箭头明确
  - 引用连线：虚线、细、双向箭头
□ 节点UI显示父子/兄弟关系
□ 连线hover显示删除按钮
```

#### 1.3 框选与快捷键
```
任务清单：
□ 启用框选多选功能
□ Delete键删除选中元素
□ Ctrl+C/V复制粘贴节点
□ 节点搜索功能（Ctrl+F）
```

### 阶段二：功能完善

**目标**：补齐设计文档中的P1功能

#### 2.1 导入功能完善
```
任务清单：
□ 实现JSON导入解析
□ 导入前数据校验
□ 导入后自动布局选项
```

#### 2.2 渲染优化
```
任务清单：
□ 代码块语法高亮
□ 流式输出光标动画
□ 大量节点性能优化
```

#### 2.3 初始体验优化
```
任务清单：
□ 空画布引导提示
□ 示例画布模板
□ 功能引导蒙层
```

### 阶段三：高级功能

**目标**：提升专业用户效率

#### 3.1 撤销/重做
```
任务清单：
□ 操作历史栈
□ Ctrl+Z/Y快捷键
□ 历史记录面板
```

#### 3.2 节点高级操作
```
任务清单：
□ 节点批量操作
□ 节点导出为图片
□ 节点分享链接
```

---

## 🏗️ 技术实现方案

### 核心依赖添加

```bash
# 自动布局算法
pnpm add dagre @types/dagre

# 或使用 elk（更强大）
pnpm add elkjs
```

### 连线类型定义

```typescript
// types/index.ts 新增
export type EdgeType = 'hierarchy' | 'reference';

export interface ChatEdge extends Edge {
  type: EdgeType;
  sourceHandle?: 'top-source' | 'bottom-source' | 'left-source' | 'right-source';
  targetHandle?: 'top-target' | 'bottom-target' | 'left-target' | 'right-target';
}
```

### 自动布局实现

```typescript
// lib/layout.ts
import dagre from 'dagre';

export function layoutTree(nodes: ChatNode[], edges: ChatEdge[]) {
  const g = new dagre.graphlib.Graph();
  g.setGraph({ rankdir: 'TB', nodesep: 100, ranksep: 150 });
  g.setDefaultEdgeLabel(() => ({}));
  
  nodes.forEach(node => {
    g.setNode(node.id, { width: 380, height: 300 });
  });
  
  edges.forEach(edge => {
    g.setEdge(edge.source, edge.target);
  });
  
  dagre.layout(g);
  
  return nodes.map(node => {
    const pos = g.node(node.id);
    return {
      ...node,
      position: { x: pos.x - 190, y: pos.y - 150 }
    };
  });
}
```

---

## 📊 优先级排序

| 排名 | 功能 | 理由 |
|------|------|------|
| 1 | 树状自动布局 | 用户明确期望，核心体验 |
| 2 | 连线类型区分 | 语义明确，关系可视化 |
| 3 | 框选多选 | 批量操作效率 |
| 4 | 快捷键支持 | 操作效率 |
| 5 | 节点搜索 | 大量节点时必需 |
| 6 | 代码高亮 | 开发者体验 |
| 7 | 导入功能 | 完整性 |
| 8 | 撤销重做 | 容错性 |
| 9 | 空画布引导 | 新用户友好 |
